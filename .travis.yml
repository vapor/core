os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode8
before_script:
  - curl -sL swift.qutheory.io/07-20 | bash
script:
  # Build Core
  - swift build
  - swift build --configuration release
  # Test Core
  - swift test
