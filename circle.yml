dependencies:
  pre:
    - eval "$(curl -sL swift.qutheory.io/travis)"

test:
  override:
    - source ~/.bashrc; swift build
    - source ~/.bashrc; swift build --configuration release
    - source ~/.bashrc; swift test
