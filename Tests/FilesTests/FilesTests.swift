import Async
import XCTest
@testable import Files

class FilesTests: XCTestCase {
    func testExample() throws {
//        let loop = try DefaultEventLoop(label: "test")
//        let file1 = try File(atPath: "/Users/path/to/main.swift", flags: [.read])
//        let file2 = try File(atPath: "/Users/path/to/kaas.swift", flags: [.mayCreate, .write])
//        let promise = Promise<Void>()
//
//        file1.source(on: loop).output(to: writer)
//
//        promise.future.await(on: loop)
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
